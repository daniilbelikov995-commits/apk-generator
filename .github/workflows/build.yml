name: Build APK

on:
  workflow_dispatch: # Позволяет запускать сборку через API (от вашего бота)

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Run Build
        run: |
          echo "Здесь пишется ваша команда сборки APK"
          # Пример для Gradle: ./gradlew assembleDebug
